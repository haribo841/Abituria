# Preserve archived source documents byte-for-byte on every platform and
# suppress synthetic whole-file diffs caused only by line endings.
/docs/legacy/originals/*.txt -text -diff

# Binary compatibility fixture generated by the historical .NET 9 application.
/tests/Abituria.Tests/Fixtures/*.db -text -diff
